# Bug Fixes - 2025-11-14

## 1. ✅ 修复API路由404错误

### 问题描述
前端调用市场数据API时，所有请求都返回404错误：
```
GET http://localhost:4000/api/market/ticker/BTC/USDC 404 (Not Found)
GET http://localhost:4000/api/market/orderbook/BTC/USDC?depth=20 404 (Not Found)
GET http://localhost:4000/api/market/klines/BTC/USDC?interval=1H&limit=100 404 (Not Found)
```

### 根本原因
Express路由中的`:symbol`参数不能包含斜杠（`/`）。当前端发送`BTC/USDC`时，Express将其视为两个路径段：
- 请求路径: `/api/market/ticker/BTC/USDC`
- 路由定义: `/api/market/ticker/:symbol`
- 实际匹配: `:symbol = BTC`, 后续的`/USDC`无法匹配

### 解决方案
对所有symbol参数进行URL编码（`encodeURIComponent`）：
- `BTC/USDC` → `BTC%2FUSDC`

### 修改文件
**frontend/src/utils/api/market.ts**
```typescript
// Before
getTicker: (symbol: string) => {
  return apiClient.get<{ data: Ticker }>(`/market/ticker/${symbol}`)
}

// After
getTicker: (symbol: string) => {
  const encodedSymbol = encodeURIComponent(symbol)
  return apiClient.get<{ data: Ticker }>(`/market/ticker/${encodedSymbol}`)
}
```

同样的修改应用于：
- `getTicker()`
- `getOrderBook()`
- `getRecentTrades()`
- `getKlines()`

---

## 2. ✅ 修复TradingChart tooltip错误

### 问题描述
```
TradingChart.vue:202 Uncaught TypeError: Cannot read properties of undefined (reading 'toFixed')
```

### 根本原因
图表tooltip的formatter函数假设数据始终是candlestick格式（4个值的数组），但在line模式下，数据是单个数字。

### 解决方案
添加类型检查，区分line和candlestick数据格式：

**frontend/src/components/trading/TradingChart.vue:252-281**
```typescript
formatter: (params: any) => {
  const data = params[0]
  if (!data) return ''
  const values = data.value

  // Handle line chart (single value)
  if (typeof values === 'number') {
    return `
      <div style="font-size: 12px;">
        <div style="margin-bottom: 4px;">${data.name}</div>
        <div style="color: #50d2c1;">Price: ${values.toFixed(2)}</div>
      </div>
    `
  }

  // Handle candlestick chart (array of OHLC values)
  if (Array.isArray(values) && values.length >= 4) {
    return `
      <div style="font-size: 12px;">
        <div style="margin-bottom: 4px;">${data.name}</div>
        <div style="color: #50d2c1;">O: ${values[0].toFixed(2)}</div>
        <div style="color: #50d2c1;">C: ${values[1].toFixed(2)}</div>
        <div style="color: #f24a67;">L: ${values[2].toFixed(2)}</div>
        <div style="color: #1fa67d;">H: ${values[3].toFixed(2)}</div>
      </div>
    `
  }

  return ''
}
```

---

## 3. ✅ 修复TradeForm balance加载错误

### 问题描述
```
Failed to load balance: TypeError: response.data.find is not a function
```

### 根本原因
API响应结构理解错误。后端返回：
```json
{
  "success": true,
  "data": [Asset[], Asset[], ...],
  "timestamp": 1234567890
}
```

ApiClient的`get()`方法返回整个response.data对象（即上面的整个JSON），但代码错误地假设response.data直接就是Asset数组。

### 解决方案
修正数据访问路径，添加安全检查：

**frontend/src/components/trading/TradeForm.vue:194-209**
```typescript
const loadBalance = async () => {
  try {
    const response = await accountApi.getBalance()
    // response is the full API response: {success, data, timestamp}
    const assets = Array.isArray(response.data) ? response.data : []
    const usdcBalance = assets.find((asset: any) => asset.asset === 'USDC')
    if (usdcBalance) {
      availableBalance.value = parseFloat(usdcBalance.availableBalance).toLocaleString('en-US', {
        minimumFractionDigits: 2,
        maximumFractionDigits: 2
      })
    }
  } catch (error) {
    console.error('Failed to load balance:', error)
  }
}
```

---

## 4. ✅ 添加Logout功能

### 新增功能
完整的用户登出功能：
- Navbar显示用户信息和logout按钮
- Logout时清除所有认证状态
- 自动跳转到登录页

### 修改文件

#### frontend/src/components/layout/Navbar.vue
- 集成auth store
- 显示当前登录用户
- 添加logout按钮和确认对话框
- 根据登录状态条件渲染UI元素

#### frontend/src/App.vue
- 应用启动时初始化auth store
- 从localStorage恢复认证状态

---

## 测试结果

✅ 所有Market API调用正常工作（symbol参数正确编码）
✅ TradingChart在candlestick和line模式切换时无错误
✅ TradeForm成功加载用户余额
✅ MarketInfo组件正常显示交易对图标
✅ Trading API路由匹配正确（/trade/orders/open）
✅ Logout功能正常工作
✅ 市场价格数据已更新为真实值（BTC: $97,121, ETH: $3,340）

---

## 注意事项

1. **Symbol编码**: 所有在URL路径中使用symbol的API都需要编码
2. **API响应结构**: ApiClient返回完整的后端响应对象，需要通过`.data`访问实际数据
3. **类型安全**: 在访问嵌套数据时添加类型检查和安全访问

---

## 5. ✅ 修复MarketInfo symbolIcon错误

### 问题描述
```
MarketInfo.vue:269 Uncaught (in promise) TypeError: Cannot read properties of undefined (reading 'substring')
```

### 根本原因
`symbolIcon` computed属性尝试访问 `currentPair.value.baseAsset.substring()`，但在组件初始加载时 `baseAsset` 可能是undefined。

### 解决方案
使用可选链操作符（optional chaining）并提供默认值：

**frontend/src/components/trading/MarketInfo.vue:268-270**
```typescript
// Before
const symbolIcon = computed(() => {
  return currentPair.value.baseAsset.substring(0, 1)
})

// After
const symbolIcon = computed(() => {
  return currentPair.value.baseAsset?.substring(0, 1) || 'B'
})
```

---

## 6. ✅ 修复Trading API路由不匹配

### 问题描述
```
GET http://localhost:4000/api/trade/orders?status=OPEN 404 (Not Found)
```

### 根本原因
前端调用 `/trade/orders?status=OPEN`，但后端路由定义是 `/trade/orders/open`，两者不匹配。

### 解决方案
修改前端API调用以匹配后端路由：

**frontend/src/utils/api/trading.ts:33-37**
```typescript
// Before
getOpenOrders: (symbol?: string) => {
  return apiClient.get<{ data: Order[] }>('/trade/orders', {
    params: { symbol, status: 'OPEN' }
  })
}

// After
getOpenOrders: (symbol?: string) => {
  return apiClient.get<{ data: Order[] }>('/trade/orders/open', {
    params: { symbol }
  })
}
```

---

## 7. ✅ 更新市场价格数据为真实值

### 问题描述
前端显示的BTC价格为50,000 USDC，与实际市场价格（约97,121 USDC）相差较大。

### 根本原因
数据库中的ticker数据是初始化时的模拟数据，未更新为真实市场价格。

### 解决方案
更新数据库中的ticker数据为当前真实市场价格（2025-11-14）：

**BTC/USDC:**
- Last Price: $97,121
- 24h Change: -6.43% (-$6,676)
- 24h High: $105,000
- 24h Low: $96,000
- 24h Volume: 78,188.45 BTC ($7.82B USDC)

**ETH/USDC:**
- Last Price: $3,340
- 24h Change: -4.20%
- 24h High: $3,520
- 24h Low: $3,300
- 24h Volume: 156,420 ETH ($540M USDC)

**修改文件:**
1. `backend/database/schema.sql` - 更新初始ticker数据
2. 执行数据库UPDATE语句更新现有数据

```sql
UPDATE tickers SET
  last_price = 97121.00,
  change_24h = -6.43,
  high_24h = 105000.00,
  low_24h = 96000.00,
  volume_24h = 78188.45,
  quote_volume_24h = 7818845307.93,
  open_price = 103797.00
WHERE symbol = 'BTC/USDC';
```

---

## 相关文件

### 前端
- `frontend/src/utils/api/market.ts` - Market API调用（Symbol URL编码）
- `frontend/src/utils/api/trading.ts` - Trading API调用（路由修复）
- `frontend/src/components/trading/TradingChart.vue` - 图表组件（Tooltip修复）
- `frontend/src/components/trading/TradeForm.vue` - 交易表单（Balance修复）
- `frontend/src/components/trading/MarketInfo.vue` - 市场信息（symbolIcon修复）
- `frontend/src/components/layout/Navbar.vue` - 导航栏（Logout功能）
- `frontend/src/App.vue` - 应用根组件（Auth初始化）
- `frontend/src/stores/auth.ts` - 认证状态管理

### 后端
- `backend/database/schema.sql` - 数据库初始化脚本（价格数据更新）
- `backend/src/services/MarketService.ts` - 市场数据服务（从数据库读取ticker）
